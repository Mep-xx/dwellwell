//dwellwell-api/tsconfig.json
{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "module": "CommonJS",
    "rootDir": "..",                 // <— broaden to repo root
    "outDir": "../build-api",        // <— emit into a sibling folder
    "composite": true,
    "types": ["node"]
  },
  "include": ["src", "../shared", "../dwellwell-client/src/utils/zillowUrl.ts"],   // <— compile api/src + shared
  "exclude": ["node_modules", "build", "../build-api"]
}